{
  "name": "dracin-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 3000",
    "build": "echo 'No build needed for worker dev'",
    "start": "wrangler dev --ip 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
